import React from 'react';
import { useForm } from 'react-hook-form';

const Login = () => {
    const {} = useForm
    return (
        <div>
            
        </div>
    );
};

export default Login;